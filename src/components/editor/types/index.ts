export interface ResizeState {
  isResizing: boolean;
  startX: number;
  startWidth: number;
}

export interface ImageElements {
  wrapper: HTMLElement;
  container: HTMLElement;
  img: HTMLImageElement;
}
